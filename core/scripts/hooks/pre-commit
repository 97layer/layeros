#!/usr/bin/env bash
# Enforce local code audit before commit.

set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"

if [[ "${SKIP_LOCAL_HOOKS:-0}" == "1" ]]; then
  exit 0
fi

bash "$REPO_ROOT/core/scripts/hooks/run_code_audit.sh" --scope staged --check paths

